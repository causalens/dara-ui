import { faAmericanSignLanguageInterpreting } from '@fortawesome/free-solid-svg-icons';

import { IconProps, StyledFAIcon } from './icon-utils';

/**
 * AmericanSignLanguageInterpreting icon from FontAwesome
 *
 * @param {IconProps} props - the component props
 */
const AmericanSignLanguageInterpreting = (props: IconProps): JSX.Element => {
    return <StyledFAIcon icon={faAmericanSignLanguageInterpreting} {...props} />;
};

export default AmericanSignLanguageInterpreting;
