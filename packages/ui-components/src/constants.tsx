export enum Key {
    BACKSPACE = 'Backspace',
    CTRL = 'Control',
    DOWN = 'ArrowDown',
    ENTER = 'Enter',
    ESCAPE = 'Escape',
    LEFT = 'ArrowLeft',
    META = 'Meta',
    MINUS = '-',
    PERIOD = '.',
    RIGHT = 'ArrowRight',
    SHIFT = 'Shift',
    TAB = 'Tab',
    UP = 'ArrowUp',
}

// A list of keys for the common controls / modifiers
export const CONTROL_KEYS: string[] = [
    Key.ESCAPE,
    Key.TAB,
    Key.ENTER,
    Key.BACKSPACE,
    Key.CTRL,
    Key.SHIFT,
    Key.UP,
    Key.DOWN,
    Key.LEFT,
    Key.RIGHT,
    Key.META,
];
